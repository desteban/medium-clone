---
interface Props {
  className?: string;
}

const { className = "" } = Astro.props;
---

<button
  class={`appearance-none py-2 px-4 bg-[#191919] text-white font-semibold rounded-full shadow-md focus:outline-none focus:ring-2 focus:ring-black focus:ring-opacity-75 text-sm font-normal ${className}`}
>
  <slot />
</button>
